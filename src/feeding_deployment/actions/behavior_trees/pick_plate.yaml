name: "PickPlate"
description: "Pick up the plate from the table."
type: "Behavior"
parameters:
  - name: "Speed"
    description: "Speed of the robot during picking up the plate."
    space:
      type: Enum
      elements: ["low", "medium", "high"]
    is_user_editable: True
    value: "medium"
fn: !hla pick_plate
